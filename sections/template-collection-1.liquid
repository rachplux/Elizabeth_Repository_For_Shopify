<div class="px-4 py-8">
  {% paginate collection.products by 6 %}
    <h1 class="text-3xl text-center">
      {{ collcetion.title }}
    </h1>
    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        {% for product in collection.products %}

          {% render 'product-card', this_product: product %}
		   {% comment %}
			上面 ' this_product: product ' => 'this_product'是自訂變數(參數)名，它將指向 內建函式'product'
			這個指令在告訴系統，帶入product card 檔案，應用其自訂的 tag name 指向 product 的屬性函式，比如 {{ this_product.title }} 就會指向 product 函數下的 title
		  {% endcomment %}

        {% else %}
          <p>There are no product inside of this collection</p>
        {% endfor %}


      </div>
    </div>
    {% render 'pagination'
      , pagination: paginate %}
	  {% comment %}
		同上，pagination 是一個(自訂)變數名，在 'pagination'檔案中出現的 pagination 就會自動帶入 paginate 函數
	  {% endcomment %}
  {% endpaginate %}
</div>